# Aura Work preview asset

The repository README displays:

`docs/assets/aura-work-preview.svg`

Replace that file with the final exported application preview before a public release while keeping the same path. Recommended dimensions: `1600x900`.

A real screenshot should avoid displaying API keys, account details, local file paths, or private project data.
